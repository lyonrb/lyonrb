= content_for :page_title, "Administration des pages"

%h2
  Toutes les pages

%ul
  - @pages.each do |page|
    %li
      = link_to page.title, edit_admin_page_path(page)
      = link_to page.permalink, page